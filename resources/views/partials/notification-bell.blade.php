{{-- Fills the bell slot the app shell declares. Its existence is the switch:
     the shell includes this partial only when it is present, so adding the bell
     needed no edit to the layout. --}}
@livewire('notifications.bell')
